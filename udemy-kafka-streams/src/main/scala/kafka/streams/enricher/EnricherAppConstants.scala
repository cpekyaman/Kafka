package kafka.streams.enricher

object EnricherAppConstants {
  val appName = "streams-userenricher"
  val producerName = "streams-userenricher-producer"

  val userTable = "userenricher-user-table"
  val purchaseStream = "userenricher-user-purchases"
}
