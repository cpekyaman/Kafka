package kafka.streams.wordcount

object WordCountAppConstants {
  val appName = "kafka-streams-wordcount"
  val inputStream = "streams-wordcount-input"
  val outputStream = "streams-wordcount-output"
}
